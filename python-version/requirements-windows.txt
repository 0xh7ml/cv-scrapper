# Windows-specific requirements for CV Scraper
# Use this file if the main requirements.txt fails on Windows

# Core dependencies
PyPDF2==3.0.1
python-docx==1.1.0
google-generativeai==0.3.2

# Alternative PDF processing (avoid PyMuPDF compilation issues)
pdfplumber==0.9.0
pypdf==3.17.4

# Text processing utilities
beautifulsoup4==4.12.2
lxml==4.9.3

# If you still want to try PyMuPDF, install pre-compiled wheel:
# pip install --only-binary=PyMuPDF PyMuPDF

# Or install from conda-forge (if using conda):
# conda install -c conda-forge pymupdf